# 1.5.9 · 2026-09-24

- 修复多显卡电脑中任务栏 GPU 与内存指标刷新时可能交换位置的问题。
- 统一采样缓存排列位置，任务栏按硬件类型与设备标识稳定排序。
- 补齐英文曲线选项翻译。

# 1.5.8 · 2026-09-24

新增应用视图与设置预览，改进字体、温度设置排列、任务栏设置配色与悬停提示；卸载可保留或删除个人数据。紧凑卡片沿用固定内部布局，附加参数位于曲线上方。减少重复扫描、排序和临时数组。验证边界见 docs/VALIDATION.md。

# 牛牛 1.5.6 · Windows

- 完整模式支持自适应参数排布：按实际窗口宽度安排多列，参数始终位于曲线上方，同一行卡片的图表对齐。
- 模块参数与模块曲线分别设置，支持参数选择、排序和恢复推荐；长内容按需滚动。
- 按硬件类型提供合适指标，区分物理硬盘、分区与网络；保留设备提供的额外传感器。
- 改善设置间距与更新窗口关闭操作；保留三种显示模式、任务栏拖动、进程图标、五档能耗影响及软件内更新。
- 复用参数控件，减少重复布局与临时分配；精简未使用的界面语言资源，保留传感器依赖及功能运行库。

Windows 10/11 x64。安装包内置运行环境，沿用原安装及数据目录可保留设置。软件内可检查、下载并安装新版本。温度项目取决于硬件及驱动支持。

安装包：NiuNiu-1.5.6-Windows-x64-Setup.exe（52,013,290 字节）
SHA256：DBF80E55199C32BBCC8B6E7018463F46D69C5BA87BFA71768C1BC099F040B24F

本次仅发布 Windows 版。

---

# 1.5.2 · 占用配色、温度通知、开机自启与名称排序

- 可选开机自启；占用页支持名称 A–Z/Z–A 排序；默认表头完整显示。
- 三种模式的硬件占用率每 10% 一档，绿 → 蓝 → 红，适配浅色与深色主题。
- 硬件模块“⋯”中的温度预警值可单独修改，三种模式共用。CPU 默认 85°C、GPU 核心 80°C、硬盘 60°C；达到阈值变红，降至阈值以下 2°C 恢复。
- 通用设置可开启 Windows 温度通知，默认关闭，并提供测试通知按钮。每次超温提醒一次，同一硬件至少间隔 5 分钟；Windows 勿扰及通知设置会影响显示。
- 更新前先完全退出旧版，沿用原程序及数据目录可保留配置。

# 1.5.0 · 中英文与 Windows 安装包；macOS 0.1.0 预览

- 增加即时中英文切换，涵盖完整/紧凑/任务栏模式、托盘菜单、设置、模块字段、曲线、占用排序和关闭提示。保持内部配置键不变，用户标题和程序/硬件等外部名称保留原文；英文紧凑导航与模块标签适配宽度。
- Windows 提供包含运行环境的 EXE 安装包，可选程序及数据目录，默认安装目录下 Data。升级复制原配置和加密密钥但保留原件；卸载不删个人数据。温度驱动仅提供可选官网下载入口，不自动安装。
- 修复本地化文本绑定为空时的边界问题；保护原有界面控件及选项值，保留既有配色、DIY、模块采样和循环滚动行为。
- Windows 原有 356 项回归与新增 32 项语言/数据检查通过；安装/升级/卸载在隔离目录验证。当前没有重新进行管理员温度实测、真实鼠标拖动和任务栏悬停/计时验证；历史结果不当作本次新验证。
- macOS 新建 Avalonia 预览程序，提供 ARM64/x64 app 压缩包及真实采集代码。10 项共享逻辑、2 项 Windows 渲染器界面检查、22 项打包检查通过；尚无 Mac 实机，温度、每程序 GPU/流量、菜单栏文字等仍有缺项。详见预览包说明，不承诺与 Windows 功能一致。
- 无 Windows 代码签名证书或 Apple 开发者账号，当前包均未获得正式签名认证。提供后续签名/公证脚本，不关闭或绕过系统防护。

# 1.4.7 · 自动滚动首尾连续循环

- 电脑参数与占用列表的自动滚动均改为末项直接衔接首项，跨越首尾时保持位移连续，去掉末尾停顿和跳回顶部。滚动更新间隔缩短至 16 毫秒，按实际经过时间计算位移。
- 循环两段共享最新采样，程序按钮仍操作正确的 PID；硬件模块、DIY 分组、节点详情和传感器展开状态保持一致，型号横向滚动同步。
- 保留五档速度、鼠标/输入/菜单暂停、手动滚动及隐藏后停止；内容放得下时不重复、不移动。原配置和旧版本保留。
- 25 项循环专项检查通过，三组等价画面的逐像素差异均为 0；临时测试程序已验证衔接段打开与关闭按钮。普通权限验证，不据此声称硬件性能压力下始终满帧。

# 1.4.6 · 紧凑占用自适应与自动滚动

- 紧凑占用按窗口高度整页轮播，显示全局排名与当前范围，始终保持从上往下的排序阅读顺序。拉高增加条数，字号与自选指标较多时减少条数；末页后返回开头，搜索或排序变化回到第一页。
- 自适应、轮播和自动滚动都支持随宽度补充同一行的占用指标，可在紧凑设置中关闭。手动勾选的指标保持可见，空间不足时显示在名称下方。
- 默认直接显示打开/关闭按钮，保留右键菜单和系统进程保护。升级时开启按钮一次，此后遵从用户自己的隐藏选择。
- 两个栏目的“滚动”均改为自动滚动，支持五档速度，到底停留后返回开头；鼠标移入、输入、菜单打开时暂停，离开后重新等待。手动滚轮仍可用，收起和切换模式停止动画；数据刷新保留滚动位置。
- 压缩紧凑导航控件，300 像素窗口配 18 号内容字体时，栏目按钮和显示方式不再挤占彼此空间。
- 新增 41 项实际界面与逻辑检查通过，包含只操作临时测试进程的打开/关闭按钮验证；悬停使用 WPF 事件派发。保留旧版与用户配置。

# 1.4.5 · 程序网络流量与紧凑多模块轮播

- 占用页增加“网络流量”，分别显示程序下载、上传速度，可按合计速率升降序排列；紧凑版可单独勾选，默认仍只显示程序名称。网络流量使用完整一行，换行时保持每个数值与单位完整。
- 通过 Windows ETW 独立采集 TCP/UDP、IPv4/IPv6 的进程流量；需要管理员权限，不将综合 I/O 当作网速。采样中、权限不足、采集失败或事件丢失明确标记；进程身份变化和 PID 重用不会继承旧计数。退出时清理自己的采集会话。
- 将界面的 I/O 改为“读写速率”，悬停解释文件、网络、设备综合读写口径；兼容旧版排序与显示选项。
- 紧凑硬件轮播根据窗口空间同时展示多个模块，每个位置轮播不同队列，不重复显示同一模块；支持自动或 1～6 个展示上限，宽高变化自动重排，全部放得下时保持静止。鼠标移入暂停，移出后重新计时。
- 23 项轮播/读写速率检查、21 项网络流量检查通过；管理员实测 TCP/UDP 的 IPv4/IPv6 收发和会话清理通过。WPF 悬停检查使用事件派发；本轮未重跑历史全套测试。独立目录升级，保留既有配置和旧版本。

# 1.4.4 · 任务栏自动轮播

- 多组任务栏指标默认每 5 秒轮播一组；右键可开关自动轮播，并选择每组停留 3 / 5 / 10 / 15 / 30 秒，显示设置的任务栏页同步提供这些选项。
- 鼠标移入或打开右键菜单时暂停，离开/关闭菜单后重新计时；滚轮和上一组/下一组仍可手动切换。单组指标保持静止，收起或退出任务栏模式后停止轮播。
- 轮播与紧凑版的轮播独立设置，采样刷新不会重置轮播计时，切换页面不会额外采样或查询公网 IP。
- 网络设置明确区分本地网卡采样（秒）与公网资料刷新（分钟）。IP、地区、ASN、运营商默认每 10 分钟查询一次；自动查询关闭时显示“仅手动刷新”。

- 普通权限 UI 与功能检查 259/259 通过，覆盖原生任务栏计时切页、采样中持续轮播、悬停/菜单暂停、手动翻页、开关/间隔保存与取消、两种网络频率独立配置。

# 1.4.3 · 任务栏交互与模块设置修复

- 牛牛任务栏区域（含文字之间的空白）接收右键，直接打开“展开完整窗口 / 显示与 DIY 设置 / 关闭…”菜单；双击可展开。通过极低透明度的输入层接收空白区域点击，保留系统任务栏背景。
- 紧凑窗口可从空白、卡片正文与数值区域拖动；按钮、下拉框、滚动条、程序条目保持原有交互，DIY 标签继续用于模块拖放。
- 修复网络模块第一次保存设置后上下行速度消失，以及编辑一种模式影响其他未配置模式的问题。界面和设置共用默认字段，明确勾选“上下行速度（下载 / 上传）”。
- 网络模块加入可按模式选择的地区、出口 IP、ASN 与运营商；数据来自 Ping0 的当前系统出口，不把它误标为各网卡独立出口。网络设置改为“网络详细信息”。
- 保存模块设置保留暂时未读到的传感器与曲线选择；取消不修改任何模式，重新打开显示当前采样值。旧版网络配置若与误保存的默认选择完全匹配且无自选参数/曲线，则恢复默认网速一次；新版主动关闭后不会再次开启。
- 普通权限 UI 与功能检查 236/236 通过，包含原生任务栏空白命中、右键菜单、真实窗口拖动、六类模块三种模式保存/切换及暂缺传感器选择保留。

# 1.4.2 · 简化牛牛图标

- 重新设计图标：石墨色圆角底、米白色抽象牛角轮廓与镂空闪电，去掉卡通面部和额外曲线。
- 窗口、程序文件、通知区域与启动快捷方式统一替换，保留 16～256 像素共 9 种尺寸与矢量原稿。
- 本次仅更新图标与版本资料；独立目录部署，保留用户设置。

# 1.4.1 · 默认分开硬盘与磁盘

- 自定义行默认按处理器、显卡、内存、硬盘、磁盘、网络排列；物理 SSD/HDD 与 C/D/E 等分区分组显示。
- “一类硬件一组”同步采用这套分组，分区模块的标签显示为“磁盘”。
- 仅自动升级未编辑过的旧默认布局，保留已自定义的标题、子行与模块位置。
- 普通权限 UI 与功能检查 163/163 通过，包含实际物理盘与分区的分配、旧默认布局升级、自定义布局保留及分组持久化。

# 1.4 · 牛牛

- 软件更名为“牛牛”，可执行文件更名为 NiuNiu.exe；窗口、托盘、快捷方式采用全新的“牛角 + 闪电 + 监控曲线”图标，含 9 种尺寸及可编辑矢量原稿。旧版配置与单实例标识继续兼容。
- 紧凑版默认窗口缩至 360×480，支持最小 300×240，记住手动调整后的宽高。
- 硬件标签与占用数字固定，型号只在显示不全时横向往返滚动，首尾停留、悬停暂停；可关闭滚动，遵从系统动画设置。
- 压缩紧凑卡片内边距、模块间距和重复信息。每模块更新时间默认隐藏，悬停可查看，设置中可重新开启。
- 紧凑占用页默认一行一个程序名称；可逐项开启 CPU、GPU、内存、I/O、能耗影响及 PID。支持搜索、排序、右键打开/关闭进程，以及可选的常驻操作按钮。
- 普通权限 UI 与功能检查 156/156 通过，验证固定标签与数字、型号滚动/悬停暂停/离开恢复/关闭停止、静止短型号、列表密度、自选字段、取消/持久化、窗口尺寸记忆及品牌图标。

# 1.3.2 更新

- 完整版和紧凑版共用整套配色，主题、强调色、窗口背景、卡片背景和文字颜色双向同步；升级时沿用原完整版配色，取消设置保持原配色。
- 模式切换、途经任务栏和重新载入配置均保留同步配色；显示内容、字体、字号及 DIY 布局继续分别保存，任务栏配色继续跟随 Windows。
- 所有下拉框统一使用垂直居中的图形箭头，色盘按钮同步修正，避免文字字符在不同字体中的位置偏差。
- 紧凑版占用排序框随字号调整宽度，搜索提示文字及密码框内容统一垂直居中。
- 普通权限 UI 与功能检查 138/138 通过，包含配色同步/迁移/取消/重新载入、完整与紧凑模式中的不同字体和字号、下拉菜单展开、设置下拉框及色盘箭头对齐。

# 1.3.1 更新

- 曲线绘图区行高由 64 压缩至 52，上方留白由 10 缩至 4；收紧文字区预留高度。保留指标名、单位、时间轴及清晰的上/中/下纵轴数值。
- Windows 任务栏指标按文字实际宽度紧密排列，取消固定等宽分配和常驻操作按钮。同样 264 像素宽的实机区域可同时显示 CPU、两块 GPU 和内存。
- 展开、显示与 DIY 设置、关闭移至任务栏右键菜单；保留双击文字展开，支持滚轮及右键菜单双向循环翻页。关闭仍先选择收起到托盘或完全退出。
- 任务栏高度不足时改用单行，长内容省略并可悬停查看；透明文字与系统配色保持一致。
- 普通权限 UI 与功能检查 109/109 通过，含窄任务栏、大字号、长指标名称、每项可翻页到达、右键菜单动作与压缩曲线刻度验证。用户配置保持原样。

# 1.3 更新

- 模块曲线可按模式多选，每条曲线标出指标、纵轴刻度/单位和真实时间轴；支持占用、温度、频率、功耗、容量与传输速度等实际可读参数。
- 程序占用表头增加可点击提示、当前排序高亮、升降序箭头，并与排序选择器同步。
- 提供 8 套完整配色及从主色自动生成明亮/深邃方案；颜色覆盖窗口、卡片、文字、按钮和图表。
- 设置明确分成完整、紧凑、任务栏及通用采样与网络页；外观、显示内容和 DIY 布局按模式独立保存，兼容原有配置。
- Windows 任务栏改为逐像素透明文字层，直接透出系统背景，文字跟随系统配色；自动避开左侧已有的可见组件。
- 统一输入框、下拉框和密码框的外形、列起点及文字缩进，修复延迟地址和水平微调文字不齐。
- 普通权限 UI 与功能检查 87/87 通过；包含实际透明任务栏、颜色/模式隔离、排序、坐标轴与对齐验证。

# 1.2 更新

- 修复深色网络节点表格的白底、低对比度文字及选中行配色；统一下拉项和辅助文字。
- 增加二维色盘、色相条、预设色与颜色值输入；强调色、窗口背景、卡片背景、文字均可 DIY，带预览和主题默认色恢复。
- 模块固定/自动宽度上限扩展为 4 格，保留组内自动换行。
- 关闭按钮左侧增加最大化/还原，同时提供最小化及标题双击；最大化按显示器工作区避开任务栏。
- 顶部压缩为两排导航；紧凑和窄窗口使用模式下拉框，布局操作收纳至菜单，状态行移至底部。
- 验证 300/440 像素宽、13/18 号字下的导航边界；保留原配置格式并增加可选配色字段。

# 1.1 更新

- 增加电脑参数 / 占用两个栏目，进程 CPU/GPU/内存/I/O/能耗影响排序和程序打开/关闭。
- 用真实系统任务栏子窗口替换旧悬浮窄条，支持任务栏宽度/偏移/主题设置。
- 补齐官方签名 PawnIO 依赖及管理员启动，恢复 Ryzen CPU 与两块 NVMe SSD 温度读取。
- 接入 Ping0 官方免费节点资料和可选高级 API 配置。
- 引入分组标题、多子行、模块拖放分配、1～3 格模块及自动换行。
- 每模块独立采样、字段选择、详细传感器与最近更新时间。
- 浅/深配色、强调色选择器、字体、字号；紧凑窗口的自适应/滚动/轮播。
- 统一十进制 KB/MB/GB 和速度单位。
- 能耗显示为注明口径的估算，非虚构每进程功率。


